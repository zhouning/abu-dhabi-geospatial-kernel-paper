# Legacy figure assets

These eight PNG files are retained only as historical artwork from the earlier
numbering scheme. They are not manuscript figures, are not regenerated by the
current renderers, and must not be selected for submission. Current figures use
the numbered `fig01_benchmark_contract` through `fig07_planning_maps_2031`
files and the `figS01` through `figS04` supplements at the parent level.
